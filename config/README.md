# Local Configuration

该目录是本独立工作区的唯一配置入口：

- `credentials.local.json`：TAPD 和其他外部系统凭证。
- `environments_config.json`：测试环境、接口域名和数据库配置。
- `connections.json`：`xjjk-yewu-sql` 使用的数据库连接登记。

所有真实配置文件均已加入 `.gitignore`，不得提交到版本库。

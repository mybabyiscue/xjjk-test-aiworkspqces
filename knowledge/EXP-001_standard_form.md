# 经验案例：EXP-001 通用表单边界校验点

- **[PC端-管理后台]**：Given 进入表单 -> When 必填项空置并保存 -> Then 前端拦截提交并展示红色高亮必填警告。
- **[PC端-管理后台]**：Given 输入字符长度限制为 L -> When 输入长度大于 L 字符 -> Then 限制保存或截断报错。
- **[PC端-管理后台]**：Given 排序值约束 >= 1 -> When 输入 0 或负数 -> Then 保存失败并提示“排序值需大于等于 1”。

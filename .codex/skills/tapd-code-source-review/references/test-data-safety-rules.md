# 测试数据安全规则

- 本技能不生成或执行数据构造 SQL；数据准备由下游 `tapd-prepare-test-from` 负责。
- 文档顶部固定声明仅限测试/预发环境，禁止生产执行。
- 只使用代码和元数据已证实的字段。
- 不得生成 `mock_val`、固定主键、推测默认值或通用 INSERT 模板。
- C/D 或冲突表只列入 `unresolved_tables.md`。
- 涉及真实用户、支付、优惠券、红包或积分时，不生成可直接触发真实资产变化的数据。

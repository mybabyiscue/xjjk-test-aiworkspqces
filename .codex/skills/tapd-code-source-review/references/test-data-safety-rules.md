# 测试数据安全规则

- 只为 A/B 级表生成 SQL 模板。
- 不执行 SQL。
- 文档顶部固定声明仅限测试/预发环境，禁止生产执行。
- 只使用代码和元数据已证实的字段。
- 未确认的 NOT NULL、唯一键、外键写 `-- TODO`，不得编造默认值。
- 清理 SQL 必须使用主键、专用 tenant_id 或唯一测试标识。
- C/D 或冲突表只列出待确认说明，不生成构造和清理 SQL。
- 涉及真实用户、支付、优惠券、红包或积分时，不生成可直接触发真实资产变化的数据。

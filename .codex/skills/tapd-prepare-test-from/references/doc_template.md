# 接口测试准备文档模板

> 本模板由 tapd-prepare-test-from 技能在生成 output/interface_test_preparation.md 时遵循。
> 所有 {{ }} 占位符必须被真实数据替换；无法获取时填 `⚠️ 待补充：[原因]`，不得留空或编造。

## 1. 文档概述
- 关联用例来源：{{ related_tables.md / related_interfaces.md 中对应的用例编号/模块名 }}
- 测试环境：{{ 平台名称 }}（api_domain: {{ api_domain }}）
- 生成时间：{{ 生成时间戳 }}

## 2. 环境前置条件清单
- 网关地址：{{ api_domain }}
- 鉴权方式与当前 Token：{{ authorization 字段值，若刚续期需注明"已通过自动登录更新" }}
- 测试账号：{{ account }}
- 数据库连接：{{ 库名、来源于项目绑定的数据库管理技能的连接标识，不写明文密码 }}
- 其他前置依赖：{{ 功能开关/白名单/第三方 mock 等，来自 related_tables.md 或 related_interfaces.md 中的备注 }}

## 3. 测试数据准备
> 对每个接口/用例分别列出

### 3.x [接口名称] 关联数据
- 数据来源：{{ 反查自 库名.表名（related_tables.md 对应条目）/ 新建预置 }}
- 反查/构造方式：
  - 若反查：{{ 实际执行的 SELECT 语句 }} → 反查结果：{{ 真实字段值 }}
  - 若预置：{{ 实际执行的 INSERT 语句（需已通过白名单校验+用户确认） }} → 生成主键：{{ 真实主键值 }}

## 4. 接口调用详情
> 每个接口一个独立小节，按依赖调用顺序排列

### 4.x [接口名称]
- HTTP Method：{{ }}
- 请求 URL：{{ api_domain + path，来自 related_interfaces.md }}
- Header：
  - Authorization: {{ 当前有效 Token }}
  - Content-Type: {{ }}
- 参数映射表：

  | 参数名 | 类型 | 是否必填 | 示例值（真实） | 取值来源 |
  |---|---|---|---|---|
  | {{ }} | {{ }} | {{ }} | {{ }} | {{ 来自哪张表/哪个前置接口返回值/固定值 }} |

- 请求体示例（完整真实 JSON，不允许出现 xxx 占位）：
```json
{{ }}
```
- 预期响应：
  - 状态码：{{ }}
  - 核心断言点：{{ 具体字段及预期值，来自用例 }}
- 依赖关系：{{ 依赖第几步接口的返回值，若无依赖写"无" }}

## 5. 测试脚本骨架
```python
# coding: utf-8
# 读取 environments_config.json -> 按依赖顺序调用接口 -> 断言校验点
```

## 6. 待确认事项清单
> 汇总全文所有 ⚠️ 待补充 标记，逐条列出，不重复正文内容

- [ ] {{ }}
